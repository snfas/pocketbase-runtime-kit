apiVersion: v1
kind: Service
metadata:
  name: {{ include "pocketbase-app.fullname" . }}
  labels:
    {{- include "pocketbase-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pocketbase-app.selectorLabels" . | nindent 4 }}

